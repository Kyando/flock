{
  "name": "flock",
  "private": true,
  "version": "0.1.0",
  "description": "A cozy sliding-tile puzzle — swipe the sheep into their pens.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "levels": "node scripts/levels-report.ts",
    "deploy": "node scripts/deploy-pages.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
